# reseau
docker network create StockService-network

# mongo api
docker run -p 9999:80
	-e ASPNETCORE_ENVIRONMENT=Production -e MONGODB_URL="mongodb://mongodb:27017" 
	-e ASPNETCORE_URLS=";http://+" -e ASPNETCORE_HTTPS_PORT=9999 
	-e ASPNETCORE_Kestrel__Certificates__Default__Password={110978} 
	-e ASPNETCORE_Kestrel__Certificates__Default__Path=/https/mongodbapi.pfx -v %USERPROFILE%\.aspnet\https:/https/ 
	--network mongoapi sebchevre/mongoapi:1.3

# mongodb	
docker run -p 27017:27017 --network StockService-network --name mongodb mongo

# keycloak
docker run --name keycloak -p 8080:8080 -e KEYCLOAK_USER=admin -e KEYCLOAK_PASSWORD=pass jboss/keycloak
